{
  # ===========================================================
  # 🧠 Terminal and Editor setup
  # ===========================================================
  programs.kitty = {
    enable = true;          # enable Kitty terminal
    settings = {
      font_size = 14;       # default terminal font size
    };
  };
}

